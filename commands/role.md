---
description: Adopt one of the framework roles for this whole session (use in a long-lived worktree workspace).
argument-hint: planner | builder | reviewer | integrator | orchestrator
---

Adopt the **$ARGUMENTS** role for the rest of this session. Read the role definition in
`~/.claude/agents/$ARGUMENTS.md` and operate under it from now on, in the current worktree.

Determine the feature you're working on from the current branch / folder — e.g. branch
`feat/<name>` -> feature folder `src/features/<name>/`, spec
`docs/features/<NAME>.md`. Confirm which feature it is, then proceed under
the role's rules.
